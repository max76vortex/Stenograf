# Legacy ASR Archive (2026-04)

This archive contains historical benchmark records from pre-Speech2Text evaluation cycles.

- `results.csv`: full legacy benchmark table snapshot moved from working area.
- `runs/`: historical raw run artifacts moved from `transcription/asr-benchmark/runs`.

Working area policy:

- `transcription/asr-benchmark/results.csv` stores only current-cycle records.
- `transcription/asr-benchmark/runs` is reserved for active provider runs.
